
\subsubsection{Les faveurs}
\label{favors}

Les faveurs jouissent de la même implantation des pouvoirs, mais sont stocké dans une \code{enum} différentes pour permettre leur exécution en début de partie.

\begin{lstlisting}[frame=single, caption={Enumération des faveurs}]
enum favor_id {
	NO_FAVOR,
	FAVOR_RETURN,
	FAVOR_RENEWAL,
	NUM_FAVOR
};
\end{lstlisting}
